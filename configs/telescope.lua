return {
  defaults = {
    layout_config = {
      preview_cutoff = nil,
      prompt_position = "bottom",
    },

  },
  -- pickers = {
  --   find_files = { theme = "dropdown" },
  --   live_grep = { theme = "dropdown" }
  -- },
}
